<!-- ABOUTME: Sticky site header — logo + The Harness wordmark on the left, mono nav + theme toggle on the right. -->
<!-- ABOUTME: Author identity lives in the manifesto + /about; social icons live in the footer. -->
---
import { Image } from 'astro:assets';
import logoIcon from '../assets/logo-icon-trimmed.png';
import HeaderLink from './HeaderLink.astro';
import ThemeToggle from './ThemeToggle.astro';
---

<header class="sticky top-0 left-0 right-0 z-50 border-b border-[--color-border]" style="background-color: var(--color-bg);">
	<nav class="page-shell flex items-center justify-between gap-3 h-16 md:h-[4.25rem]">
		<a
			href="/"
			class="flex min-w-0 items-center gap-2 no-underline hover:no-underline"
		>
			<Image src={logoIcon} alt="" width={28} height={40} class="logo-icon" />
			<span class="font-heading font-medium text-lg sm:text-xl text-[--color-text-primary] tracking-[-0.02em] truncate">The Harness</span>
		</a>

		<div class="flex shrink-0 items-center gap-0.5 md:gap-2">
			<HeaderLink href="/blog">writing</HeaderLink>
			<span class="text-[--color-text-tertiary] text-xs hidden md:inline">&middot;</span>
			<HeaderLink href="/about">about</HeaderLink>
			<span class="text-[--color-text-tertiary] text-xs hidden md:inline">&middot;</span>
			<HeaderLink href="/subscribe">subscribe</HeaderLink>
			<span class="text-[--color-text-tertiary] text-xs hidden md:inline">&middot;</span>
			<HeaderLink href="/search">search</HeaderLink>
			<ThemeToggle />
		</div>
	</nav>
</header>

<style>
	.logo-icon {
		filter: drop-shadow(0 0 4px rgba(232, 168, 76, 0.4));
	}
</style>
